Bug (confirmed in production): every Landowner created since the starting package shipped has two `resources` rows for type "grain". routes/characters.ts inserts grain "0" as the landowner class resource (classResourceByProfession), then grantStartingPackage (services/character.ts:43) inserts a second grain row at "10" with a plain insert. /me/state balances and settleGoods let the last row win (the UI shows 10 and farm output banks there), while getOrCreateResource uses .limit(1) with no ORDER BY and returns the 0 row. So the player market list, the vendor sell and the staff food draw all hit the empty row. Symptom: POST /api/market/list returns 409 "You hold only 0 wheat" while the UI shows 10 Wheat.

Do:
1. grantStartingPackage: credit the starting grain through getOrCreateResource plus a relative credit, never a raw insert. Keep the slave-class early return.
2. getOrCreateResource (buildings.ts:206 and the private copy in barracks.ts:161): INSERT ... ON CONFLICT (scope, scope_id, type) DO NOTHING, then select the row. Keep the signatures. Make holdings.ts creditGood and every other insert(resources) path (grep apps/server, apps/worker, packages) safe against an existing row the same way.
3. Migration 0061_resources_unique.sql, in one transaction: for each (scope, scope_id, type) group with more than one row, keep the row with the latest last_updated_at (tie-break on id), add the other rows' amounts to it, delete the rest. Then CREATE UNIQUE INDEX resources_scope_type_uq ON resources (scope, scope_id, type). Mirror the index in packages/db/src/schema.ts.
4. Tests: creating a Landowner yields exactly one grain row holding 10; that Landowner can list grain on the player market and sell grain to the vendor; the staff food draw takes from that row; two concurrent getOrCreateResource calls for one player and type leave one row; the migration's merge sums a seeded duplicate pair into the row with the latest last_updated_at.
5. Change nothing else. Follow AGENTS.md: invariants, one item per commit staged by path. Run the full gate, and when it's green push to main so CI deploys; the Railway boot step applies 0061.

Report back: the commits, the gate result, and confirmation that 0061 is idempotent on a database with no duplicates.
